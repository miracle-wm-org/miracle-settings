name: miracle_wm_settings
description: "A settings application for miracle-wm, a tiling window manager for Wayland."
publish_to: 'none'
version: 0.0.1

environment:
  sdk: ^3.0.0

dependencies:
  flutter:
    sdk: flutter
  ffi: ^2.1.4
  process_run: ^0.12.0
  flutter_colorpicker: ^1.1.0
  logging: ^1.3.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
  generate: true
